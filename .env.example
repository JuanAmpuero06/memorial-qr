# Memorial QR - Variables de Entorno
# ================================
# COPIA este archivo como .env y configura tus valores
# NUNCA subas el archivo .env al repositorio
# ================================

# ========================================
# BACKEND
# ========================================

# Base de datos PostgreSQL
DATABASE_URL=postgresql://postgres:tu_password_seguro@db:5432/memorial_qr

# Clave secreta para JWT (genera una nueva para producción)
# Puedes generar una con: openssl rand -hex 32
SECRET_KEY=cambiar-esta-clave-en-produccion-minimo-32-caracteres

# Algoritmo JWT
ALGORITHM=HS256

# Duración del token en minutos
ACCESS_TOKEN_EXPIRE_MINUTES=30

# URLs (con Traefik, todo pasa por puerto 80)
BACKEND_URL=http://localhost
FRONTEND_URL=http://localhost

# Entorno (development, production)
ENVIRONMENT=development

# ========================================
# POSTGRES
# ========================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=tu_password_seguro
POSTGRES_DB=memorial_qr

# ========================================
# PGADMIN (Opcional)
# ========================================
PGADMIN_DEFAULT_EMAIL=admin@memorial-qr.com
PGADMIN_DEFAULT_PASSWORD=cambiar_password_admin

# ========================================
# FRONTEND
# ========================================
# Con Traefik, la API está en /api
VITE_API_URL=http://localhost/api
